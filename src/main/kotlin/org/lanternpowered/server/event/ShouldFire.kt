/*
 * Lantern
 *
 * Copyright (c) LanternPowered <https://www.lanternpowered.org>
 * Copyright (c) SpongePowered <https://www.spongepowered.org>
 * Copyright (c) contributors
 *
 * This work is licensed under the terms of the MIT License (MIT). For
 * a copy, see 'LICENSE.txt' or <https://opensource.org/licenses/MIT>.
 */
package org.lanternpowered.server.event

object ShouldFire {

    /*
    @ShouldFireTarget(AITaskEvent.Add.class)
    public static boolean AI_TASK_EVENT_ADD = false;
    @ShouldFireTarget(AITaskEvent.Remove.class)
    public static boolean AI_TASK_EVENT_REMOVE = false;

    @ShouldFireTarget(SpawnEntityEvent.class)
    public static boolean SPAWN_ENTITY_EVENT = false;
    @ShouldFireTarget(SpawnEntityEvent.ChunkLoad.class)
    public static boolean SPAWN_ENTITY_EVENT_CHUNK_LOAD = false;
    @ShouldFireTarget(SpawnEntityEvent.Custom.class)
    public static boolean SPAWN_ENTITY_EVENT_CUSTOM = false;

    @ShouldFireTarget(ChangeBlockEvent.class)
    public static boolean CHANGE_BLOCK_EVENT = false;
    @ShouldFireTarget(ChangeBlockEvent.Break.class)
    public static boolean CHANGE_BLOCK_EVENT_BREAK = false;
    @ShouldFireTarget(ChangeBlockEvent.Place.class)
    public static boolean CHANGE_BLOCK_EVENT_PLACE = false;
    @ShouldFireTarget(ChangeBlockEvent.Post.class)
    public static boolean CHANGE_BLOCK_EVENT_POST = false;

    @ShouldFireTarget(DropItemEvent.class)
    public static boolean DROP_ITEM_EVENT = false;
    @ShouldFireTarget(DropItemEvent.Destruct.class)
    public static boolean DROP_ITEM_EVENT_DESTRUCT = false;
    @ShouldFireTarget(DropItemEvent.Dispense.class)
    public static boolean DROP_ITEM_EVENT_DISPENSE = false;

    @ShouldFireTarget(RideEntityEvent.class)
    public static boolean RIDE_ENTITY_EVENT = false;
    @ShouldFireTarget(RideEntityEvent.Mount.class)
    public static boolean RIDE_ENTITY_EVENT_MOUNT = false;
    @ShouldFireTarget(RideEntityEvent.Dismount.class)
    public static boolean RIDE_ENTITY_EVENT_DISMOUNT = false;

    @ShouldFireTarget(PrimeExplosiveEvent.Pre.class)
    public static boolean PRIME_EXPLOSIVE_EVENT_PRE = false;
    @ShouldFireTarget(PrimeExplosiveEvent.Post.class)
    public static boolean PRIME_EXPLOSIVE_EVENT_POST = false;

    @ShouldFireTarget(DefuseExplosiveEvent.Pre.class)
    public static boolean DEFUSE_EXPLOSIVE_EVENT_PRE = false;
    @ShouldFireTarget(DefuseExplosiveEvent.Post.class)
    public static boolean DEFUSE_EXPLOSIVE_EVENT_POST = false;

    @ShouldFireTarget(SetAITargetEvent.class)
    public static boolean SET_AI_TARGET_EVENT = false;

    @ShouldFireTarget(ChangeInventoryEvent.Transfer.Pre.class)
    public static boolean CHANGE_INVENTORY_EVENT_TRANSFER_PRE = false;
    @ShouldFireTarget(ChangeInventoryEvent.Transfer.Post.class)
    public static boolean CHANGE_INVENTORY_EVENT_TRANSFER_POST = false;

    @ShouldFireTarget(TickBlockEvent.class)
    public static boolean TICK_BLOCK_EVENT = false;
    */

}
